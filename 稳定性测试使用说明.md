# AS-Maker-Prep 长时间运行稳定性测试

## 概述

`test_stability_long_running.js` 是一个专门设计用于验证 AS-Maker-Prep 系统长时间运行稳定性的测试脚本。该脚本通过持续的 WebSocket 数据流监控和间歇性的 REST API 请求，全面测试系统在长期运行环境下的稳定性和可靠性。

## 🎯 测试目标

### 第一步：澄清测试目标 (Clarify the Scope)

本稳定性测试的核心目标是验证以下关键功能在长时间运行环境下的表现：

1. **WebSocket 连接稳定性**：验证 WebSocket 连接能够长时间保持稳定，自动处理断线重连
2. **数据流持续性**：确保价格数据、订单簿、余额和持仓信息能够持续、准确地接收
3. **REST API 可靠性**：通过间歇性的 API 请求验证 REST 接口的长期稳定性
4. **系统资源管理**：监控内存使用、连接状态等系统资源的健康状况
5. **错误恢复能力**：测试系统在遇到网络波动、API 限制等异常情况时的自动恢复能力

## 📋 测试策略 (Plan the Strategy)

### 核心功能测试 (Positive Scenarios)
- ✅ WebSocket 数据流持续接收验证
- ✅ REST API 定期请求成功率监控
- ✅ 账户数据（余额、持仓）实时同步
- ✅ 市场数据（价格、订单簿）实时更新

### 异常与边界测试 (Negative & Edge Cases)
- ✅ 网络断线自动重连机制
- ✅ API 请求失败重试逻辑
- ✅ 错误阈值达到时的安全停止
- ✅ 内存泄漏和资源占用监控

### 性能与稳定性测试 (Performance & Stability)
- ✅ 长时间运行内存使用趋势
- ✅ API 响应时间统计分析
- ✅ 数据接收频率和质量监控
- ✅ 系统健康状态定期检查

### 监控与报告 (Monitoring & Reporting)
- ✅ 实时运行状态日志
- ✅ 定期统计报告生成
- ✅ 异常情况告警机制
- ✅ 优雅退出和数据保存

## 🚀 使用方法

### 启动测试

```bash
# 进入项目目录
cd f:\api\AS-Maker-Prep

# 启动长时间稳定性测试
node test_stability_long_running.js
```

### 测试配置

测试脚本会自动读取 `config.json` 中配置的交易对，并使用 `.env` 文件中的 API 凭证。

**关键配置参数：**
- `restApiInterval`: REST API 请求间隔（默认 30 秒）
- `healthCheckInterval`: 健康检查间隔（默认 60 秒）
- `statsReportInterval`: 统计报告间隔（默认 5 分钟）
- `maxRestApiErrors`: 最大 REST API 错误数（默认 10 次）
- `maxWebSocketErrors`: 最大 WebSocket 错误数（默认 20 次）

## 📊 测试用例设计 (Design Test Cases)

| 用例ID | 测试标题 | 前置条件 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|----------|
| ST-001 | WebSocket 连接初始化 | 配置文件正确，网络正常 | 启动测试脚本 | WebSocket 成功连接并订阅数据流 |
| ST-002 | Ticker 数据持续接收 | WebSocket 已连接 | 等待数据接收 | 每秒接收多条 Ticker 数据，每 100 条记录一次日志 |
| ST-003 | OrderBook 数据接收 | WebSocket 已连接 | 监控订单簿数据 | 每 50 条 OrderBook 数据记录一次日志 |
| ST-004 | REST API 定期测试 | 交易所服务已初始化 | 每 30 秒执行一次 API 请求 | 成功获取余额、持仓和价格数据 |
| ST-005 | 健康检查机制 | 系统正常运行 | 每 60 秒执行健康检查 | 记录运行时间、内存使用和连接状态 |
| ST-006 | 统计报告生成 | 测试运行中 | 每 5 分钟生成报告 | 输出详细的数据统计和性能指标 |
| ST-007 | 错误处理机制 | 模拟网络异常 | 触发连接错误 | 自动重连并记录错误次数 |
| ST-008 | 优雅退出机制 | 测试正在运行 | 按 Ctrl+C 或发送 SIGTERM | 清理资源并生成最终报告 |

## ⚡ WebSocket 延迟监控功能

### 延迟计算原理

稳定性测试脚本现在包含了全面的WebSocket延迟监控功能：

1. **时间戳提取**：尝试从接收到的数据中提取原始时间戳
2. **传输延迟计算**：计算数据从交易所发送到本地接收的时间差
3. **估算机制**：当无法获取准确时间戳时，使用网络延迟估算
4. **异常值过滤**：自动过滤不合理的延迟值（超过5秒的异常值）

### 延迟统计指标

- **平均延迟**：所有样本的平均值，反映整体网络性能
- **最小延迟**：记录的最低延迟值，表示最佳网络条件
- **最大延迟**：记录的最高延迟值，用于识别网络波动
- **样本数量**：保留最近1000个延迟样本进行统计

### 延迟警告机制

- **平均延迟警告**：当平均延迟超过200ms时发出警告
- **最大延迟警告**：当最大延迟超过1000ms时发出警告
- **实时监控**：在健康检查中显示当前延迟状态

### 延迟数据分类

脚本分别统计以下类型数据的延迟：
- **Ticker延迟**：价格数据的传输延迟
- **OrderBook延迟**：订单簿数据的传输延迟
- **Balance延迟**：账户余额更新的延迟
- **Positions延迟**：持仓信息更新的延迟
- **总体延迟**：所有类型数据的综合延迟统计

## 📈 监控指标

### WebSocket 数据统计
- **Ticker 数据**：接收条数、最后接收时间、错误次数、延迟统计
- **OrderBook 数据**：接收条数、最后接收时间、错误次数、延迟统计
- **Balance 数据**：更新次数、最后更新时间、延迟统计
- **Positions 数据**：更新次数、最后更新时间、延迟统计
- **重连次数**：WebSocket 自动重连统计
- **总错误数**：WebSocket 相关错误累计
- **延迟监控**：实时计算和统计WebSocket数据传输延迟

### REST API 统计
- **总请求数**：发起的 API 请求总数
- **成功次数**：成功响应的请求数
- **失败次数**：失败的请求数
- **成功率**：成功请求占总请求的百分比
- **平均响应时间**：API 请求的平均响应时间
- **错误记录**：详细的错误信息和时间戳

### 系统性能指标
- **运行时间**：测试脚本的总运行时长
- **内存使用**：堆内存使用情况的历史记录
- **健康检查次数**：系统健康检查的执行次数
- **连接状态**：WebSocket 连接的当前状态

## 🔍 日志输出说明

### 启动阶段日志
```
[INFO] === 长时间运行稳定性测试初始化 ===
[INFO] 测试交易对: BTC/USDT:USDT, ETH/USDT:USDT
[INFO] ✅ 交易所服务初始化成功
[INFO] ✅ WebSocket服务初始化成功
[INFO] 🚀 稳定性测试初始化完成
```

### 数据接收日志
```
[INFO] [Ticker-100] ETH/USDT:USDT: 3665.56 (延迟: 45.2ms, 平均: 38.7ms)
[INFO] [OrderBook-50] ETH/USDT:USDT: 买一=3666.21, 卖一=3667.64 (延迟: 52.1ms, 平均: 41.3ms)
[INFO] [Balance-1] 余额更新 (延迟: 28.5ms, 平均: 28.5ms)
[INFO] [Positions-1] 持仓更新 (延迟: 31.2ms, 平均: 31.2ms)
```

### REST API 测试日志
```
[INFO] 🔄 执行REST API测试 [第1次]
[INFO] 💰 账户余额: USDT=1167.69476165
[INFO] 📊 当前持仓: 0 个
[INFO] 📈 BTC/USDT:USDT 价格: 118333.8
[INFO] ✅ REST API测试成功 (响应时间: 910ms)
```

### 健康检查日志
```
[INFO] 💓 健康检查 [第1次]
[INFO] ⏱️ 运行时间: 5分钟 30秒
[INFO] 🧠 内存使用: 45MB / 67MB
[INFO] 🔗 WebSocket状态: open
[INFO] ⚡ 当前延迟: 平均=42.3ms, 最大=156.7ms
```

### 统计报告日志
```
[INFO] 📊 ===== 稳定性测试统计报告 =====
[INFO] 🕐 测试运行时间: 15分钟 45秒
[INFO] 📡 WebSocket数据统计:
[INFO]   - Ticker: 1250 条
[INFO]   - OrderBook: 625 条
[INFO]   - Balance: 2 条
[INFO]   - Positions: 2 条
[INFO]   - 重连次数: 0
[INFO]   - 错误次数: 0
[INFO] ⚡ WebSocket延迟统计:
[INFO]   - 总体延迟: 平均=41.2ms, 最小=15.3ms, 最大=187.6ms
[INFO]   - Ticker延迟: 平均=39.8ms, 最小=18.2ms, 最大=156.4ms
[INFO]   - Orderbook延迟: 平均=43.7ms, 最小=15.3ms, 最大=187.6ms
[INFO]   - Balance延迟: 平均=28.5ms, 最小=28.5ms, 最大=28.5ms
[INFO]   - Positions延迟: 平均=31.2ms, 最小=31.2ms, 最大=31.2ms
[INFO] 🌐 REST API统计:
[INFO]   - 总请求: 32
[INFO]   - 成功: 32
[INFO]   - 失败: 0
[INFO]   - 成功率: 100.00%
[INFO]   - 平均响应时间: 856ms
```

## ⚠️ 注意事项

### 运行环境要求
1. **网络连接**：确保网络连接稳定，能够访问交易所 API
2. **API 凭证**：确保 `.env` 文件中的 API 凭证正确且有效
3. **系统资源**：建议在资源充足的环境下运行，监控内存使用情况
4. **运行时间**：建议至少运行 1-2 小时以获得有意义的稳定性数据
5. **测试环境**：建议在模拟盘环境下进行测试，避免影响实盘交易
6. **错误恢复**：脚本具备自动错误恢复机制，WebSocket连接错误不会导致测试中断

### 停止测试
- **正常停止**：按 `Ctrl+C` 触发优雅退出
- **强制停止**：发送 `SIGTERM` 信号
- **自动停止**：当错误次数超过阈值时自动停止

### 故障排除
1. **WebSocket 连接失败**：检查网络连接和 API 凭证
2. **数据接收中断**：查看错误日志，可能是网络波动导致
3. **REST API 失败**：检查 API 限制和凭证有效性
4. **内存使用过高**：考虑调整统计数据保留策略

## 🔧 错误处理与恢复机制

### 自动错误恢复

脚本内置了强大的错误处理和自动恢复机制：

- **WebSocket错误容错**：订阅失败、连接断开等错误不会导致脚本退出
- **自动重连**：WebSocket连接断开时自动重连，支持指数退避策略
- **网络错误处理**：网络超时、连接拒绝等错误会被自动处理
- **Promise错误捕获**：未处理的Promise拒绝会被捕获并分类处理
- **API限流处理**：遇到交易所API限流时会自动重试

### 错误分类处理

脚本会根据错误类型采取不同的处理策略：

1. **可恢复错误** (继续运行):
   - WebSocket相关错误 (连接、订阅、watch操作)
   - 网络错误 (ECONNREFUSED, ENOTFOUND, ETIMEDOUT, ECONNRESET, timeout, fetch failed, socket hang up)
   - 交易所API错误 (bitget, exchange, API, rate limit, insufficient, invalid)
   - 订阅和重连相关错误

2. **严重错误** (停止测试):
   - 配置文件错误
   - 系统资源不足
   - 未知的致命错误

### 重连策略

- **重连次数**：最多10次重连尝试
- **重连间隔**：1s, 2s, 4s, 8s, 16s, 32s, 60s（指数退避）
- **重连成功**：自动重新订阅所有数据流，等待2秒确保连接稳定
- **重连失败**：达到最大次数后停止测试

### Promise错误处理优化

- **Promise.allSettled**：所有WebSocket订阅使用Promise.allSettled确保错误被正确捕获
- **错误堆栈分析**：unhandledRejection处理器会分析错误堆栈，精确识别错误来源
- **分类日志**：根据错误类型输出不同级别的日志信息

## 🔧 最新优化记录

### 2025-01-22 WebSocket错误处理优化

**问题描述**:
- 脚本在长时间运行过程中会因为WebSocket订阅错误而停止
- 出现大量"未处理的Promise拒绝"错误
- unhandledRejection处理器无法正确识别WebSocket相关错误

**优化措施**:
1. **Promise处理优化**:
   - 将所有WebSocket订阅改为使用`Promise.allSettled`
   - 确保每个订阅操作都有明确的错误处理
   - 避免产生未处理的Promise拒绝

2. **错误识别增强**:
   - 增加错误堆栈分析，检查`websocket_manager`、`watchTicker`等关键词
   - 扩展网络错误识别范围，包括`ETIMEDOUT`、`ECONNRESET`、`fetch failed`等
   - 新增交易所API错误识别，包括`bitget`、`rate limit`等

3. **重连机制完善**:
   - 重连成功后等待2秒确保连接稳定
   - 重连后完整重新订阅所有数据流

**优化效果**:
- 脚本能够稳定运行，不再因WebSocket错误而意外停止
- 错误处理更加精确，减少误判
- 提高了长期稳定性测试的可靠性

---

## 📋 测试执行指南 (Explain & Execute)

### 执行步骤

1. **环境准备**
   ```bash
   # 确保依赖已安装
   npm install
   
   # 检查配置文件
   cat config.json
   cat .env
   ```

2. **启动测试**
   ```bash
   # 启动稳定性测试
   node test_stability_long_running.js
   ```

3. **监控运行**
   - 观察初始化日志确保所有服务正常启动
   - 监控数据接收频率和 REST API 成功率
   - 关注内存使用和系统健康状态

4. **收集结果**
   - 让测试运行足够长的时间（建议 1-2 小时）
   - 记录统计报告中的关键指标
   - 分析任何异常或错误模式

### 重要测试场景

**场景 1：正常运行稳定性**
- 在网络稳定环境下运行 2 小时
- 验证数据接收的连续性和准确性
- 确认 REST API 成功率保持在 95% 以上

**场景 2：网络波动恢复能力**
- 在测试过程中短暂断开网络连接
- 验证 WebSocket 自动重连机制
- 确认数据接收能够快速恢复

**场景 3：长期内存稳定性**
- 运行 4-6 小时监控内存使用趋势
- 确认没有明显的内存泄漏
- 验证系统资源使用保持在合理范围

## 📊 总结与扩展 (Summarize & Extend)

### 测试覆盖度总结

本稳定性测试脚本全面覆盖了以下关键领域：

✅ **连接稳定性**：WebSocket 长连接和 REST API 间歇性请求  
✅ **数据完整性**：价格、订单簿、余额、持仓数据的持续监控  
✅ **错误处理**：自动重连、错误阈值、优雅退出机制  
✅ **性能监控**：内存使用、响应时间、数据接收频率  
✅ **系统健康**：定期健康检查和统计报告生成  

### 当前测试策略的优势

1. **全面性**：覆盖了 WebSocket 和 REST API 两种主要通信方式
2. **实时性**：提供实时的运行状态和性能指标监控
3. **自动化**：无需人工干预，能够自动处理常见异常情况
4. **可观测性**：详细的日志记录和统计报告便于问题诊断
5. **安全性**：具备错误阈值保护和优雅退出机制

### 潜在盲点和扩展建议

**当前可能存在的盲点：**

1. **高频交易场景**：当前测试主要针对中低频数据，对于高频交易场景的压力测试覆盖不足
2. **并发连接测试**：没有测试多个 WebSocket 连接同时运行的情况
3. **数据准确性验证**：主要关注数据接收，对数据准确性的验证有限
4. **API 限制边界**：没有专门测试 API 调用频率限制的边界情况

**下一步扩展建议：**

1. **压力测试模块**：
   ```bash
   # 建议开发高频数据压力测试
   node test_high_frequency_stress.js
   ```

2. **数据准确性验证**：
   - 添加数据一致性检查机制
   - 对比 WebSocket 和 REST API 数据的一致性
   - 验证价格数据的合理性范围

3. **并发连接测试**：
   - 测试多个交易对同时订阅的性能
   - 验证系统在高并发情况下的稳定性

4. **故障注入测试**：
   - 模拟各种网络异常情况
   - 测试系统在极端条件下的恢复能力

5. **性能基准测试**：
   - 建立性能基准指标
   - 定期对比性能变化趋势

通过这个长时间运行稳定性测试，我们能够有效验证 AS-Maker-Prep 系统在生产环境中的可靠性和稳定性，为后续的策略开发和部署提供坚实的基础保障。